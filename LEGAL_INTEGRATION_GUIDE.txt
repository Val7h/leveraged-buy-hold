═══════════════════════════════════════════════════════════════════════════════
LEGAL INTEGRATION GUIDE - COMPLETE WORKFLOW
═══════════════════════════════════════════════════════════════════════════════

STATUS: READY FOR TUESDAY EXECUTION
ESTIMATED TIME: 8 hours
CONFIDENCE: 70%

═══════════════════════════════════════════════════════════════════════════════

PARTE 1: DATABASE MIGRATION (1 HOUR)
═══════════════════════════════════════════════════════════════════════════════

FILE: backend/migrations/003_add_consent_fields.py
STATUS: ✅ READY

STEPS:
1. Open file: backend/migrations/003_add_consent_fields.py
2. Review the migration:
   ```python
   def upgrade():
       op.add_column('users', sa.Column('risk_acknowledged', sa.Boolean, default=False))
       op.add_column('users', sa.Column('terms_accepted', sa.Boolean, default=False))
       op.add_column('users', sa.Column('consent_logged_at', sa.DateTime, nullable=True))
   ```

3. Run migration in development:
   ```bash
   cd backend
   alembic upgrade head
   ```

4. Verify columns added:
   ```sql
   SELECT * FROM users LIMIT 1;
   -- Should show: risk_acknowledged, terms_accepted, consent_logged_at
   ```

5. Test downgrade (optional):
   ```bash
   alembic downgrade -1
   alembic upgrade head
   ```

EXPECTED RESULT:
✅ Three new columns added to users table
✅ Default values set correctly
✅ Migrations reversible

═══════════════════════════════════════════════════════════════════════════════

PARTE 2: BACKEND ENDPOINT (1.5-2 HOURS)
═══════════════════════════════════════════════════════════════════════════════

FILE: backend/app/api/v1/user_consent.py
STATUS: ✅ CODE COMPLETE (just needs database connection)

ENDPOINT: POST /api/v1/user/consent

WHAT TO DO:
1. Open file: backend/app/api/v1/user_consent.py
2. You'll see:
   - ConsentData model (acceptRisk, acceptTerms, timestamp)
   - log_consent function with complete implementation
   - Error handling and validation
   - TODO comments showing where to uncomment for production

3. Uncomment production code when ready:
   - Uncomment "# from app.database import get_db"
   - Uncomment "# from app.auth import get_current_user"
   - Uncomment "# from app.models import User"
   - Uncomment the PRODUCTION IMPLEMENTATION section
   - Comment out the TESTING/STAGING section

4. Test the endpoint:

TESTING WITHOUT DATABASE (development):
```bash
curl -X POST http://localhost:8000/api/v1/user/consent \
  -H "Content-Type: application/json" \
  -d '{
    "acceptRisk": true,
    "acceptTerms": true,
    "timestamp": "2026-06-18T14:30:00Z"
  }'

EXPECTED RESPONSE:
{
  "success": true,
  "message": "Consent logged successfully (staging mode)",
  "timestamp": "2026-06-18T14:30:00Z",
  "data": {
    "user_id": "user_123",
    "risk_acknowledged": true,
    "terms_accepted": true,
    "logged_at": "2026-06-18T14:30:00Z",
    "note": "In production, this will be persisted to database"
  }
}
```

TESTING WITH DATABASE (production):
```bash
# After uncommenting production code
curl -X POST http://localhost:8000/api/v1/user/consent \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "acceptRisk": true,
    "acceptTerms": true,
    "timestamp": "2026-06-18T14:30:00Z"
  }'
```

EXPECTED RESULT:
✅ Endpoint receives POST requests
✅ Validates input data (both checkboxes must be true)
✅ Validates timestamp format
✅ Saves to database (when connected)
✅ Returns 200 with success flag

═══════════════════════════════════════════════════════════════════════════════

PARTE 3: FRONTEND-BACKEND INTEGRATION (2-2.5 HOURS)
═══════════════════════════════════════════════════════════════════════════════

FILE: frontend/src/components/RiskDisclaimerModal.tsx
STATUS: ✅ READY (already created)

FILE: frontend/src/app/login/page.tsx
STATUS: ✅ INTEGRATED (already added)

WHAT'S ALREADY DONE:
✅ RiskDisclaimerModal component created
✅ Modal imported in login/page.tsx
✅ Modal shows after successful registration
✅ Modal blocks navigation until both checkboxes accepted

WHAT NEEDS TO BE ADDED:
Connect the modal submit button to the backend endpoint.

WHERE TO ADD IT:
In RiskDisclaimerModal.tsx, find the submit handler:

```typescript
// Current (likely empty or placeholder):
const handleSubmit = () => {
  // TODO: Post consent to backend
}

// Should become:
const handleSubmit = async () => {
  try {
    const response = await fetch('/api/v1/user/consent', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${authToken}` // Get from auth context
      },
      body: JSON.stringify({
        acceptRisk: checkboxRisk,
        acceptTerms: checkboxTerms,
        timestamp: new Date().toISOString()
      })
    })

    if (response.ok) {
      const data = await response.json()
      console.log('Consent logged:', data)
      onAccept() // Call parent handler to close modal
    } else {
      console.error('Failed to log consent:', response.statusText)
      // Show error to user
    }
  } catch (error) {
    console.error('Error logging consent:', error)
    // Show error to user
  }
}
```

TESTING INTEGRATION:
1. Start both frontend and backend dev servers
2. Go to http://localhost:3000/login
3. Click "Criar Conta" (register tab)
4. Fill in form (name, email, password, risk profile)
5. Click "Criar Conta" button
6. RiskDisclaimerModal should appear
7. Check both checkboxes
8. Click "Aceitar" button
9. Modal should submit to backend
10. Should redirect to dashboard

═══════════════════════════════════════════════════════════════════════════════

PARTE 4: MOBILE TESTING (1.5-2 HOURS)
═══════════════════════════════════════════════════════════════════════════════

DEVICES TO TEST:
[ ] iPhone 12 (390 x 844)
[ ] iPhone 13 (390 x 844)
[ ] Android (360 x 720)
[ ] Tablet (768 x 1024)

WHAT TO TEST:
[ ] Modal displays correctly on mobile
[ ] Modal doesn't overflow screen
[ ] Both checkboxes visible and easily clickable
[ ] Submit button is large enough (44x44px minimum)
[ ] Close button accessible (if any)
[ ] Form text readable (16px minimum)
[ ] No horizontal scrolling
[ ] Confirm button works on mobile

TESTING STEPS:
1. Open Chrome DevTools (F12)
2. Toggle responsive mode (Ctrl+Shift+M)
3. Select iPhone 12 from device list
4. Go to http://localhost:3000/login
5. Register new user
6. Test modal on mobile:
   - Visibility of all elements
   - Checkbox interaction
   - Submit button
   - Verify POST request in Network tab

═══════════════════════════════════════════════════════════════════════════════

PARTE 5: ERROR HANDLING & EDGE CASES (1.5 HOURS)
═══════════════════════════════════════════════════════════════════════════════

TEST THESE SCENARIOS:

1. INVALID TIMESTAMPS:
   Input: "invalid-timestamp"
   Expected: 400 error "Invalid timestamp format"
   ✓ Code handles this

2. UNCHECKED BOXES:
   Input: acceptRisk=false, acceptTerms=true
   Expected: 400 error "Both checkboxes must be true"
   ✓ Code handles this

3. FUTURE TIMESTAMP:
   Input: timestamp = tomorrow's date
   Expected: 400 error "Cannot be in future"
   ✓ Code handles this

4. MISSING AUTH:
   Input: No Authorization header
   Expected: 401 error "Not authenticated" (production)
   ✓ Code ready for this

5. DATABASE ERROR:
   Input: Database is down
   Expected: 500 error with message
   ✓ Code handles this with rollback

═══════════════════════════════════════════════════════════════════════════════

ESTIMATED TIMELINE - TUESDAY (9 AM - 5 PM)
═══════════════════════════════════════════════════════════════════════════════

9:00-10:00 AM: Database migration (1 hour)
  [ ] Run alembic upgrade
  [ ] Verify columns added
  [ ] Test downgrade/upgrade

10:00 AM-12:00: Endpoint implementation & testing (2 hours)
  [ ] Uncomment production code in user_consent.py
  [ ] Connect to database
  [ ] Test with curl
  [ ] Verify data saved in database

12:00-1:00 PM: LUNCH

1:00-2:30 PM: Frontend integration (1.5 hours)
  [ ] Update RiskDisclaimerModal submit handler
  [ ] Connect to backend endpoint
  [ ] Test flow: register → modal → submit → dashboard
  [ ] Debug any issues

2:30-4:30 PM: Mobile testing (2 hours)
  [ ] Test on iPhone 12 size
  [ ] Test on Android size
  [ ] Test on tablet size
  [ ] Fix any responsive issues

4:30-5:00 PM: Documentation & cleanup (30 min)
  [ ] Document findings
  [ ] Record confidence level
  [ ] Note any blockers
  [ ] Commit to git

═══════════════════════════════════════════════════════════════════════════════

SUCCESS CRITERIA
═══════════════════════════════════════════════════════════════════════════════

MUST HAVE (to mark COMPLETE):
✅ Database migration runs successfully
✅ Endpoint receives POST requests
✅ Modal appears on login page
✅ Both checkboxes required (can't submit without them)
✅ Consent data logged to database
✅ Mobile tested (iPhone + Android)
✅ No console errors
✅ User redirects to dashboard after consent

NICE TO HAVE:
⏳ Performance: Endpoint <100ms response time
⏳ Accessibility: Modal passes WCAG audit
⏳ Analytics: Track consent submissions
⏳ Retry logic: Handle failed submissions

═══════════════════════════════════════════════════════════════════════════════

COMMON ISSUES & SOLUTIONS
═══════════════════════════════════════════════════════════════════════════════

ISSUE: "ModuleNotFoundError: No module named 'app.database'"
FIX: Check that database imports are correct in your project structure
     Adjust imports to match your actual module organization

ISSUE: "User not authenticated" error
FIX: Make sure you're passing Authorization header with valid token
     Check that auth context is working

ISSUE: Modal doesn't submit
FIX: Check Network tab in Chrome DevTools
     Verify endpoint URL is correct (/api/v1/user/consent)
     Check request headers (Content-Type, Authorization)

ISSUE: Mobile layout broken
FIX: Check RiskDisclaimerModal component styling
     Ensure max-width is set (e.g., max-w-md)
     Ensure padding accounts for mobile (px-4)

═══════════════════════════════════════════════════════════════════════════════

FILES CHECKLIST - TUESDAY MORNING
═══════════════════════════════════════════════════════════════════════════════

Before starting, verify these files exist:
☐ backend/migrations/003_add_consent_fields.py
☐ backend/app/api/v1/user_consent.py
☐ frontend/src/components/RiskDisclaimerModal.tsx
☐ frontend/src/app/login/page.tsx

All files should be ready to go!

═══════════════════════════════════════════════════════════════════════════════

NEXT STEPS AFTER TUESDAY
═══════════════════════════════════════════════════════════════════════════════

WEDNESDAY: QA Testing
- Lighthouse audit
- Performance verification
- End-to-end testing
- Bug fixes

THURSDAY: GO/NO-GO Decision
- Review 5 checkpoints
- Vote GO or NO-GO
- Deploy (if GO) or activate June 26 buffer (if NO-GO)

═══════════════════════════════════════════════════════════════════════════════

YOU GOT THIS! 💪

All code is ready.
All documentation is complete.
Just execute the steps on Tuesday.

═══════════════════════════════════════════════════════════════════════════════
